---
/**
 * Contact CTA Section Component
 * A high-converting contact section positioned at the bottom of pages to capture leads
 * from users before they exit. Targets local businesses with persuasive copy
 * and a clear call-to-action.
 *
 * Features:
 * - Mobile-first responsive design
 * - Clear visual hierarchy: headline -> supporting text -> trust signals -> CTA
 * - WCAG 2.1 AA compliant contrast and touch targets
 * - Semantic HTML structure for accessibility and SEO
 * - Trust signals for increased conversions
 * - Click tracking for conversion analysis
 * - Contrasting background to stand out from surrounding content
 *
 * @example Basic usage
 * ```astro
 * <ContactCTA
 *   headline="Ready to Grow Your Business Online?"
 *   supportingText="Join dozens of successful local businesses who trust us..."
 *   primaryCTA={{ text: "Get Your Free Quote", href: "/contact" }}
 * />
 * ```
 */

import type { ContactCTAProps } from '../types/contact-cta';
import Button from './Button.astro';

interface Props extends ContactCTAProps {}

const {
  headline,
  supportingText,
  primaryCTA,
  secondaryCTA,
  trustSignals = [],
  id = 'contact-cta',
  class: className = '',
  ariaLabel = 'Contact us for a free consultation',
  contrast = true,
} = Astro.props;

// Build CSS classes
const sectionClasses = [
  'contact-cta',
  contrast && 'contact-cta--contrast',
  className,
].filter(Boolean).join(' ');

// Default trust signals if none provided
const defaultTrustSignals = [
  {
    value: '50+',
    label: 'Projects Completed',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="24" height="24"><path d="M9 16.17L4.83 12l-1.42 1.41L9 19 21 7l-1.41-1.41L9 16.17z"/></svg>`,
  },
  {
    value: '10+',
    label: 'Years Experience',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="24" height="24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-1 17.93c-3.95-.49-7-3.85-7-7.93 0-.62.08-1.21.21-1.79L9 15v1c0 1.1.9 2 2 2v1.93zm6.9-2.54c-.26-.81-1-1.39-1.9-1.39h-1v-3c0-.55-.45-1-1-1H8v-2h2c.55 0 1-.45 1-1V7h2c1.1 0 2-.9 2-2v-.41c2.93 1.19 5 4.06 5 7.41 0 2.08-.8 3.97-2.1 5.39z"/></svg>`,
  },
  {
    value: 'Your Area',
    label: 'Proudly Serving',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" width="24" height="24"><path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/></svg>`,
  },
];

const displayTrustSignals = trustSignals.length > 0 ? trustSignals : defaultTrustSignals;
---

<section
  class={sectionClasses}
  id={id}
  aria-labelledby={`${id}-headline`}
  aria-label={ariaLabel}
  data-section="contact-cta"
>
  <div class="contact-cta__container">
    <div class="contact-cta__content">
      <!-- Main Headline -->
      <h2 id={`${id}-headline`} class="contact-cta__headline">
        {headline}
      </h2>

      <!-- Supporting Text -->
      <p class="contact-cta__text">
        {supportingText}
      </p>

      <!-- Trust Signals -->
      {displayTrustSignals.length > 0 && (
        <div class="contact-cta__trust-signals" role="list" aria-label="Why businesses trust us">
          {displayTrustSignals.map((signal) => (
            <div class="contact-cta__trust-signal" role="listitem">
              {signal.icon && (
                <span class="contact-cta__trust-icon" aria-hidden="true" set:html={signal.icon} />
              )}
              <span class="contact-cta__trust-value">{signal.value}</span>
              <span class="contact-cta__trust-label">{signal.label}</span>
            </div>
          ))}
        </div>
      )}

      <!-- CTA Buttons -->
      <div class="contact-cta__buttons">
        <Button
          text={primaryCTA.text}
          href={primaryCTA.href}
          variant={primaryCTA.variant || 'primary'}
          size="large"
          class="contact-cta__btn contact-cta__btn--primary"
          id="contact-cta-primary-btn"
          ariaDescribedBy={`${id}-headline`}
        />
        {secondaryCTA && (
          <Button
            text={secondaryCTA.text}
            href={secondaryCTA.href}
            variant={secondaryCTA.variant || 'secondary'}
            size="large"
            class="contact-cta__btn contact-cta__btn--secondary"
            id="contact-cta-secondary-btn"
          />
        )}
      </div>
    </div>
  </div>

  <!-- Decorative background element -->
  <div class="contact-cta__background" aria-hidden="true">
    <div class="contact-cta__bg-pattern"></div>
    <div class="contact-cta__bg-glow contact-cta__bg-glow--1"></div>
    <div class="contact-cta__bg-glow contact-cta__bg-glow--2"></div>
  </div>

  <!-- Fancy Interior Border -->
  <div class="contact-cta__interior-border" aria-hidden="true">
    <div class="contact-cta__border-line contact-cta__border-line--top"></div>
    <div class="contact-cta__border-line contact-cta__border-line--right"></div>
    <div class="contact-cta__border-line contact-cta__border-line--bottom"></div>
    <div class="contact-cta__border-line contact-cta__border-line--left"></div>
    <div class="contact-cta__border-corner contact-cta__border-corner--top-left">
      <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M0 40V0H40" stroke="currentColor" stroke-width="2" fill="none"/>
        <circle cx="0" cy="0" r="4" fill="currentColor"/>
        <path d="M8 0L0 8" stroke="currentColor" stroke-width="1.5"/>
        <path d="M16 0L0 16" stroke="currentColor" stroke-width="1" opacity="0.6"/>
      </svg>
    </div>
    <div class="contact-cta__border-corner contact-cta__border-corner--top-right">
      <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M40 40V0H0" stroke="currentColor" stroke-width="2" fill="none"/>
        <circle cx="40" cy="0" r="4" fill="currentColor"/>
        <path d="M32 0L40 8" stroke="currentColor" stroke-width="1.5"/>
        <path d="M24 0L40 16" stroke="currentColor" stroke-width="1" opacity="0.6"/>
      </svg>
    </div>
    <div class="contact-cta__border-corner contact-cta__border-corner--bottom-left">
      <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M0 0V40H40" stroke="currentColor" stroke-width="2" fill="none"/>
        <circle cx="0" cy="40" r="4" fill="currentColor"/>
        <path d="M8 40L0 32" stroke="currentColor" stroke-width="1.5"/>
        <path d="M16 40L0 24" stroke="currentColor" stroke-width="1" opacity="0.6"/>
      </svg>
    </div>
    <div class="contact-cta__border-corner contact-cta__border-corner--bottom-right">
      <svg viewBox="0 0 40 40" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M40 0V40H0" stroke="currentColor" stroke-width="2" fill="none"/>
        <circle cx="40" cy="40" r="4" fill="currentColor"/>
        <path d="M32 40L40 32" stroke="currentColor" stroke-width="1.5"/>
        <path d="M24 40L40 24" stroke="currentColor" stroke-width="1" opacity="0.6"/>
      </svg>
    </div>
    <div class="contact-cta__border-diamond contact-cta__border-diamond--top">
      <svg viewBox="0 0 16 16" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M8 0L16 8L8 16L0 8L8 0Z" fill="currentColor"/>
      </svg>
    </div>
  </div>
</section>

<style>
  /* =================================================================
   * CONTACT CTA SECTION COMPONENT STYLES
   * =================================================================
   * Mobile-first approach with responsive breakpoints
   * Uses design system variables from variables.css
   * Follows BEM naming convention for maintainability
   */

  .contact-cta {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-16) var(--spacing-4);
    overflow: hidden;
    background-color: var(--color-background-muted);
  }

  /* Contrast variant - darker, more prominent */
  .contact-cta--contrast {
    background: linear-gradient(
      135deg,
      var(--color-primary-800) 0%,
      var(--color-primary-900) 50%,
      var(--color-primary-950) 100%
    );
  }

  /* Decorative background container */
  .contact-cta__background {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  /* Animated dot/grid pattern background */
  .contact-cta__bg-pattern {
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(
        circle at center,
        rgba(255, 255, 255, 0.08) 1px,
        transparent 1px
      );
    background-size: 32px 32px;
    opacity: 0;
    transition: opacity var(--duration-500) var(--ease-out);
  }

  .contact-cta--contrast .contact-cta__bg-pattern {
    opacity: 1;
    animation: ctaPatternFloat 20s linear infinite;
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__bg-pattern {
    background-image:
      radial-gradient(
        circle at center,
        var(--color-primary-200) 1px,
        transparent 1px
      );
    opacity: 0.3;
  }

  /* Animated glow orbs */
  .contact-cta__bg-glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0;
    transition: opacity var(--duration-700) var(--ease-out);
  }

  .contact-cta--contrast .contact-cta__bg-glow {
    opacity: 1;
  }

  .contact-cta__bg-glow--1 {
    width: 500px;
    height: 500px;
    top: -150px;
    left: -100px;
    background: radial-gradient(
      circle,
      rgba(139, 92, 246, 0.25) 0%,
      transparent 70%
    );
    animation: ctaGlowPulse1 8s ease-in-out infinite;
  }

  .contact-cta__bg-glow--2 {
    width: 400px;
    height: 400px;
    bottom: -100px;
    right: -50px;
    background: radial-gradient(
      circle,
      rgba(6, 182, 212, 0.2) 0%,
      transparent 70%
    );
    animation: ctaGlowPulse2 10s ease-in-out infinite;
  }

  /* Non-contrast variant glow adjustments */
  .contact-cta:not(.contact-cta--contrast) .contact-cta__bg-glow--1 {
    opacity: 0.5;
    background: radial-gradient(
      circle,
      rgba(54, 83, 149, 0.15) 0%,
      transparent 70%
    );
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__bg-glow--2 {
    opacity: 0.4;
    background: radial-gradient(
      circle,
      rgba(139, 92, 246, 0.12) 0%,
      transparent 70%
    );
  }

  /* Background pattern animation */
  @keyframes ctaPatternFloat {
    0% {
      background-position: 0 0;
    }
    100% {
      background-position: 32px 32px;
    }
  }

  /* Glow pulse animations */
  @keyframes ctaGlowPulse1 {
    0%, 100% {
      transform: translate(0, 0) scale(1);
      opacity: 0.25;
    }
    50% {
      transform: translate(30px, 20px) scale(1.1);
      opacity: 0.35;
    }
  }

  @keyframes ctaGlowPulse2 {
    0%, 100% {
      transform: translate(0, 0) scale(1);
      opacity: 0.2;
    }
    50% {
      transform: translate(-20px, -15px) scale(1.15);
      opacity: 0.3;
    }
  }

  .contact-cta__container {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: var(--container-6xl);
    margin: 0 auto;
  }

  .contact-cta__content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--spacing-6);
  }

  /* =================================================================
   * TYPOGRAPHY - Mobile First
   * =================================================================
   */

  /* Main Headline - H2 */
  .contact-cta__headline {
    font-family: var(--font-heading);
    font-size: var(--font-size-2xl);
    font-weight: var(--font-weight-bold);
    line-height: var(--line-height-tight);
    letter-spacing: var(--letter-spacing-tight);
    color: var(--color-text-primary);
    margin: 0;
    max-width: 20ch;
  }

  .contact-cta--contrast .contact-cta__headline {
    color: var(--color-text-inverse);
  }

  /* Supporting Text */
  .contact-cta__text {
    font-family: var(--font-body);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-normal);
    line-height: var(--line-height-relaxed);
    color: var(--color-text-secondary);
    margin: 0;
    max-width: 55ch;
  }

  .contact-cta--contrast .contact-cta__text {
    color: var(--color-primary-100);
    opacity: 0.95;
  }

  /* =================================================================
   * TRUST SIGNALS
   * =================================================================
   */

  .contact-cta__trust-signals {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-4);
    padding: var(--spacing-6) 0;
    width: 100%;
  }

  .contact-cta__trust-signal {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-1);
    padding: var(--spacing-3) var(--spacing-4);
    background: rgba(255, 255, 255, 0.08);
    border-radius: var(--radius-xl);
    border: 1px solid rgba(255, 255, 255, 0.12);
    min-width: 140px;
    overflow: hidden;
    transition:
      transform var(--duration-300) var(--ease-out),
      background-color var(--duration-300) var(--ease-out),
      border-color var(--duration-300) var(--ease-out),
      box-shadow var(--duration-300) var(--ease-out);
  }

  /* Shine effect overlay for trust signals */
  .contact-cta__trust-signal::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 50%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(255, 255, 255, 0.1) 50%,
      transparent 100%
    );
    transform: skewX(-20deg);
    transition: left var(--duration-700) var(--ease-out);
    pointer-events: none;
  }

  /* Subtle gradient accent at top */
  .contact-cta__trust-signal::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(
      90deg,
      var(--color-accent) 0%,
      var(--color-accent-alt) 100%
    );
    opacity: 0;
    transition: opacity var(--duration-300) var(--ease-out);
  }

  /* Hover states for contrast variant */
  .contact-cta--contrast .contact-cta__trust-signal:hover {
    background: rgba(255, 255, 255, 0.12);
    border-color: rgba(255, 255, 255, 0.2);
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
  }

  .contact-cta--contrast .contact-cta__trust-signal:hover::before {
    left: 150%;
  }

  .contact-cta--contrast .contact-cta__trust-signal:hover::after {
    opacity: 0.8;
  }

  /* Non-contrast variant styling */
  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-signal {
    background: var(--color-background);
    border: 1px solid var(--color-border);
    box-shadow: var(--shadow-sm);
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-signal::before {
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(54, 83, 149, 0.08) 50%,
      transparent 100%
    );
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-signal::after {
    background: linear-gradient(
      90deg,
      var(--color-primary) 0%,
      var(--color-accent) 100%
    );
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-signal:hover {
    background: var(--color-background-subtle);
    border-color: var(--color-primary-200);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md), 0 0 0 1px rgba(54, 83, 149, 0.1);
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-signal:hover::before {
    left: 150%;
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-signal:hover::after {
    opacity: 1;
  }

  /* Icon container with subtle glow on hover */
  .contact-cta__trust-icon {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    color: var(--color-primary-300);
    margin-bottom: var(--spacing-1);
    border-radius: var(--radius-full);
    background: rgba(255, 255, 255, 0.05);
    transition:
      background-color var(--duration-300) var(--ease-out),
      transform var(--duration-300) var(--ease-out),
      box-shadow var(--duration-300) var(--ease-out);
  }

  .contact-cta--contrast .contact-cta__trust-signal:hover .contact-cta__trust-icon {
    background: rgba(255, 255, 255, 0.1);
    transform: scale(1.1);
    box-shadow: 0 0 20px rgba(139, 92, 246, 0.3);
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-icon {
    color: var(--color-primary);
    background: var(--color-primary-50);
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-signal:hover .contact-cta__trust-icon {
    background: var(--color-primary-100);
    transform: scale(1.1);
    box-shadow: var(--glow-primary-sm);
  }

  .contact-cta__trust-icon :global(svg) {
    width: 1.25rem;
    height: 1.25rem;
    transition: transform var(--duration-300) var(--ease-out);
  }

  .contact-cta__trust-signal:hover .contact-cta__trust-icon :global(svg) {
    transform: scale(1.05);
  }

  .contact-cta__trust-value {
    font-family: var(--font-heading);
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    color: var(--color-text-inverse);
    line-height: var(--line-height-none);
    transition: transform var(--duration-300) var(--ease-out);
  }

  .contact-cta__trust-signal:hover .contact-cta__trust-value {
    transform: scale(1.02);
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-value {
    color: var(--color-primary);
  }

  .contact-cta__trust-label {
    font-family: var(--font-body);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-medium);
    color: var(--color-primary-200);
    text-transform: uppercase;
    letter-spacing: var(--letter-spacing-wider);
    opacity: 0.9;
    transition: opacity var(--duration-300) var(--ease-out);
  }

  .contact-cta__trust-signal:hover .contact-cta__trust-label {
    opacity: 1;
  }

  .contact-cta:not(.contact-cta--contrast) .contact-cta__trust-label {
    color: var(--color-text-tertiary);
  }

  /* =================================================================
   * CTA BUTTONS
   * =================================================================
   */

  .contact-cta__buttons {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-4);
    margin-top: var(--spacing-2);
    width: 100%;
  }

  /* Full width CTAs on mobile */
  .contact-cta__btn {
    width: 100%;
    max-width: 20rem;
  }

  /* Override button styles for contrast variant */
  .contact-cta--contrast :global(.btn--primary) {
    background-color: var(--color-background);
    color: var(--color-primary-700);
    box-shadow: var(--shadow-lg);
  }

  .contact-cta--contrast :global(.btn--primary:hover:not(.btn--disabled)) {
    background-color: var(--color-primary-50);
    color: var(--color-primary-800);
    box-shadow: var(--shadow-xl);
    transform: translateY(-2px);
  }

  .contact-cta--contrast :global(.btn--primary:active:not(.btn--disabled)) {
    background-color: var(--color-primary-100);
    transform: translateY(0);
  }

  .contact-cta--contrast :global(.btn--primary:focus-visible) {
    box-shadow:
      0 0 0 var(--focus-ring-offset) var(--color-primary-800),
      0 0 0 calc(var(--focus-ring-offset) + var(--focus-ring-width)) var(--color-background);
  }

  .contact-cta--contrast :global(.btn--secondary) {
    background-color: transparent;
    color: var(--color-text-inverse);
    border-color: rgba(255, 255, 255, 0.5);
  }

  .contact-cta--contrast :global(.btn--secondary:hover:not(.btn--disabled)) {
    background-color: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.8);
    color: var(--color-text-inverse);
  }

  /* =================================================================
   * FANCY INTERIOR BORDER
   * =================================================================
   * An elegant white ornamental border that frames the CTA section
   * with decorative corners and diamond accents (matches HeroSection)
   */

  .contact-cta__interior-border {
    position: absolute;
    inset: var(--spacing-4);
    pointer-events: none;
    z-index: 0;
    opacity: 0;
    transition: opacity var(--duration-500) var(--ease-out);
  }

  .contact-cta--contrast .contact-cta__interior-border {
    opacity: 1;
  }

  /* Border lines with gradient fade effect */
  .contact-cta__border-line {
    position: absolute;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(255, 255, 255, 0.15) 10%,
      rgba(255, 255, 255, 0.4) 50%,
      rgba(255, 255, 255, 0.15) 90%,
      transparent 100%
    );
  }

  .contact-cta__border-line--top,
  .contact-cta__border-line--bottom {
    left: 50px;
    right: 50px;
    height: 1px;
  }

  .contact-cta__border-line--top {
    top: 0;
  }

  .contact-cta__border-line--bottom {
    bottom: 0;
  }

  .contact-cta__border-line--left,
  .contact-cta__border-line--right {
    top: 50px;
    bottom: 50px;
    width: 1px;
    background: linear-gradient(
      180deg,
      transparent 0%,
      rgba(255, 255, 255, 0.15) 10%,
      rgba(255, 255, 255, 0.4) 50%,
      rgba(255, 255, 255, 0.15) 90%,
      transparent 100%
    );
  }

  .contact-cta__border-line--left {
    left: 0;
  }

  .contact-cta__border-line--right {
    right: 0;
  }

  /* Decorative corner ornaments */
  .contact-cta__border-corner {
    position: absolute;
    width: 40px;
    height: 40px;
    color: rgba(255, 255, 255, 0.5);
    transition: color var(--duration-300) var(--ease-out);
  }

  .contact-cta__border-corner svg {
    width: 100%;
    height: 100%;
  }

  .contact-cta__border-corner--top-left {
    top: 0;
    left: 0;
  }

  .contact-cta__border-corner--top-right {
    top: 0;
    right: 0;
  }

  .contact-cta__border-corner--bottom-left {
    bottom: 0;
    left: 0;
  }

  .contact-cta__border-corner--bottom-right {
    bottom: 0;
    right: 0;
  }

  /* Diamond accent ornaments */
  .contact-cta__border-diamond {
    position: absolute;
    width: 12px;
    height: 12px;
    left: 50%;
    transform: translateX(-50%);
    color: rgba(255, 255, 255, 0.6);
    filter: drop-shadow(0 0 4px rgba(255, 255, 255, 0.3));
    animation: ctaDiamondPulse 3s ease-in-out infinite;
  }

  .contact-cta__border-diamond svg {
    width: 100%;
    height: 100%;
  }

  .contact-cta__border-diamond--top {
    top: -6px;
  }

  @keyframes ctaDiamondPulse {
    0%, 100% {
      opacity: 0.6;
      transform: translateX(-50%) scale(1);
    }
    50% {
      opacity: 1;
      transform: translateX(-50%) scale(1.2);
    }
  }

  /* =================================================================
   * RESPONSIVE STYLES - Tablet (640px+)
   * =================================================================
   */

  @media screen and (min-width: 640px) {
    .contact-cta {
      padding: var(--spacing-20) var(--spacing-6);
    }

    .contact-cta__interior-border {
      inset: var(--spacing-6);
    }

    .contact-cta__border-corner {
      width: 50px;
      height: 50px;
    }

    .contact-cta__border-line--top,
    .contact-cta__border-line--bottom {
      left: 60px;
      right: 60px;
    }

    .contact-cta__border-line--left,
    .contact-cta__border-line--right {
      top: 60px;
      bottom: 60px;
    }

    .contact-cta__border-diamond {
      width: 14px;
      height: 14px;
    }

    .contact-cta__border-diamond--top {
      top: -7px;
    }

    .contact-cta__content {
      gap: var(--spacing-6);
    }

    .contact-cta__headline {
      font-size: var(--font-size-3xl);
      max-width: 22ch;
    }

    .contact-cta__text {
      font-size: var(--font-size-md);
    }

    .contact-cta__trust-signals {
      flex-direction: row;
      justify-content: center;
      flex-wrap: wrap;
      gap: var(--spacing-6);
    }

    .contact-cta__trust-signal {
      padding: var(--spacing-4) var(--spacing-6);
      min-width: 160px;
    }

    .contact-cta__trust-icon {
      width: 2.75rem;
      height: 2.75rem;
    }

    .contact-cta__trust-icon :global(svg) {
      width: 1.375rem;
      height: 1.375rem;
    }

    .contact-cta__buttons {
      flex-direction: row;
      justify-content: center;
      margin-top: var(--spacing-4);
    }

    .contact-cta__btn {
      width: auto;
    }
  }

  /* =================================================================
   * RESPONSIVE STYLES - Desktop (1024px+)
   * =================================================================
   */

  @media screen and (min-width: 1024px) {
    .contact-cta {
      padding: var(--spacing-24) var(--spacing-8);
    }

    .contact-cta__content {
      gap: var(--spacing-8);
    }

    .contact-cta__headline {
      font-size: var(--font-size-4xl);
      max-width: 25ch;
    }

    .contact-cta__text {
      font-size: var(--font-size-lg);
      max-width: 60ch;
    }

    .contact-cta__trust-signals {
      gap: var(--spacing-8);
      padding: var(--spacing-8) 0;
    }

    .contact-cta__trust-signal {
      padding: var(--spacing-5) var(--spacing-8);
    }

    .contact-cta__trust-icon {
      width: 3rem;
      height: 3rem;
    }

    .contact-cta__trust-icon :global(svg) {
      width: 1.5rem;
      height: 1.5rem;
    }

    .contact-cta__trust-value {
      font-size: var(--font-size-2xl);
    }

    .contact-cta__trust-label {
      font-size: var(--font-size-sm);
    }

    .contact-cta__buttons {
      gap: var(--spacing-6);
      margin-top: var(--spacing-6);
    }

    /* Interior border - desktop enhancements */
    .contact-cta__interior-border {
      inset: var(--spacing-8);
    }

    .contact-cta__border-corner {
      width: 60px;
      height: 60px;
      color: rgba(255, 255, 255, 0.55);
    }

    .contact-cta__border-line {
      background: linear-gradient(
        90deg,
        transparent 0%,
        rgba(255, 255, 255, 0.2) 15%,
        rgba(255, 255, 255, 0.5) 50%,
        rgba(255, 255, 255, 0.2) 85%,
        transparent 100%
      );
    }

    .contact-cta__border-line--left,
    .contact-cta__border-line--right {
      background: linear-gradient(
        180deg,
        transparent 0%,
        rgba(255, 255, 255, 0.2) 15%,
        rgba(255, 255, 255, 0.5) 50%,
        rgba(255, 255, 255, 0.2) 85%,
        transparent 100%
      );
    }

    .contact-cta__border-line--top,
    .contact-cta__border-line--bottom {
      left: 80px;
      right: 80px;
    }

    .contact-cta__border-line--left,
    .contact-cta__border-line--right {
      top: 80px;
      bottom: 80px;
    }

    .contact-cta__border-diamond {
      width: 16px;
      height: 16px;
    }

    .contact-cta__border-diamond--top {
      top: -8px;
    }
  }

  /* =================================================================
   * RESPONSIVE STYLES - Large Desktop (1280px+)
   * =================================================================
   */

  @media screen and (min-width: 1280px) {
    .contact-cta__headline {
      font-size: var(--font-size-5xl);
    }

    .contact-cta__text {
      font-size: var(--font-size-xl);
    }

    .contact-cta__trust-value {
      font-size: var(--font-size-3xl);
    }

    .contact-cta__trust-label {
      font-size: var(--font-size-base);
    }
  }

  /* =================================================================
   * ANIMATIONS
   * Subtle fade-in animation
   * =================================================================
   */

  .contact-cta__headline {
    animation: ctaFadeIn 0.6s ease-out 0.1s backwards;
  }

  .contact-cta__text {
    animation: ctaFadeIn 0.6s ease-out 0.2s backwards;
  }

  .contact-cta__trust-signals {
    animation: ctaFadeIn 0.6s ease-out 0.3s backwards;
  }

  .contact-cta__buttons {
    animation: ctaFadeIn 0.6s ease-out 0.4s backwards;
  }

  @keyframes ctaFadeIn {
    from {
      opacity: 0;
      transform: translateY(15px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* =================================================================
   * REDUCED MOTION
   * Respect user preferences for reduced motion
   * =================================================================
   */

  @media (prefers-reduced-motion: reduce) {
    .contact-cta__headline,
    .contact-cta__text,
    .contact-cta__trust-signals,
    .contact-cta__buttons {
      animation: none;
    }

    .contact-cta :global(.btn:hover:not(.btn--disabled)) {
      transform: none;
    }

    /* Disable background animations */
    .contact-cta__bg-pattern {
      animation: none;
    }

    .contact-cta__bg-glow--1,
    .contact-cta__bg-glow--2 {
      animation: none;
    }

    /* Disable trust signal hover transforms */
    .contact-cta__trust-signal:hover {
      transform: none;
    }

    .contact-cta__trust-signal::before {
      display: none;
    }

    .contact-cta__trust-signal:hover .contact-cta__trust-icon {
      transform: none;
    }

    .contact-cta__trust-signal:hover .contact-cta__trust-value {
      transform: none;
    }

    .contact-cta__trust-signal:hover .contact-cta__trust-icon :global(svg) {
      transform: none;
    }

    /* Disable interior border diamond animation */
    .contact-cta__border-diamond {
      animation: none;
    }
  }

  /* =================================================================
   * HIGH CONTRAST MODE
   * Ensure visibility in Windows High Contrast mode
   * =================================================================
   */

  @media (forced-colors: active) {
    .contact-cta {
      background: Canvas;
      border: 2px solid CanvasText;
    }

    .contact-cta__headline,
    .contact-cta__text {
      color: CanvasText;
    }

    .contact-cta__trust-signal {
      border: 1px solid CanvasText;
      background: transparent;
    }

    .contact-cta__trust-signal::before,
    .contact-cta__trust-signal::after {
      display: none;
    }

    .contact-cta__trust-icon {
      background: transparent;
      border: 1px solid CanvasText;
    }

    .contact-cta__trust-value,
    .contact-cta__trust-label {
      color: CanvasText;
    }

    .contact-cta__background {
      display: none;
    }

    .contact-cta__interior-border {
      display: none;
    }
  }

  /* =================================================================
   * PRINT STYLES
   * =================================================================
   */

  @media print {
    .contact-cta {
      padding: var(--spacing-8) 0;
      background: transparent;
      border-top: 1px solid var(--color-border);
    }

    .contact-cta__headline,
    .contact-cta__text {
      color: var(--color-text-primary);
      animation: none;
    }

    .contact-cta__trust-signals {
      animation: none;
    }

    .contact-cta__trust-signal {
      background: transparent;
      border: 1px solid var(--color-border);
    }

    .contact-cta__trust-signal::before,
    .contact-cta__trust-signal::after {
      display: none;
    }

    .contact-cta__trust-icon {
      background: transparent;
      border: 1px solid var(--color-border);
    }

    .contact-cta__trust-value,
    .contact-cta__trust-label {
      color: var(--color-text-primary);
    }

    .contact-cta__background {
      display: none;
    }

    .contact-cta__buttons {
      display: none;
    }

    .contact-cta__interior-border {
      display: none;
    }
  }
</style>

<script>
  /**
   * CTA Button Click Tracking
   * Tracks clicks on CTA buttons for conversion analysis.
   * Uses the data layer pattern for compatibility with analytics tools.
   */
  document.addEventListener('DOMContentLoaded', () => {
    const primaryBtn = document.getElementById('contact-cta-primary-btn');
    const secondaryBtn = document.getElementById('contact-cta-secondary-btn');

    // Track primary CTA clicks
    if (primaryBtn) {
      primaryBtn.addEventListener('click', () => {
        // Push to dataLayer if available (Google Tag Manager)
        if (typeof window !== 'undefined' && (window as any).dataLayer) {
          (window as any).dataLayer.push({
            event: 'cta_click',
            cta_location: 'contact_cta_section',
            cta_type: 'primary',
            cta_text: primaryBtn.textContent?.trim(),
            cta_destination: primaryBtn.getAttribute('href'),
          });
        }

        // Custom event for other analytics integrations
        window.dispatchEvent(new CustomEvent('contact-cta-click', {
          detail: {
            type: 'primary',
            text: primaryBtn.textContent?.trim(),
            destination: primaryBtn.getAttribute('href'),
            timestamp: new Date().toISOString(),
          }
        }));

        // Console log for development
        if (import.meta.env.DEV) {
          console.log('[ContactCTA] Primary CTA clicked:', {
            text: primaryBtn.textContent?.trim(),
            destination: primaryBtn.getAttribute('href'),
          });
        }
      });
    }

    // Track secondary CTA clicks
    if (secondaryBtn) {
      secondaryBtn.addEventListener('click', () => {
        // Push to dataLayer if available (Google Tag Manager)
        if (typeof window !== 'undefined' && (window as any).dataLayer) {
          (window as any).dataLayer.push({
            event: 'cta_click',
            cta_location: 'contact_cta_section',
            cta_type: 'secondary',
            cta_text: secondaryBtn.textContent?.trim(),
            cta_destination: secondaryBtn.getAttribute('href'),
          });
        }

        // Custom event for other analytics integrations
        window.dispatchEvent(new CustomEvent('contact-cta-click', {
          detail: {
            type: 'secondary',
            text: secondaryBtn.textContent?.trim(),
            destination: secondaryBtn.getAttribute('href'),
            timestamp: new Date().toISOString(),
          }
        }));

        // Console log for development
        if (import.meta.env.DEV) {
          console.log('[ContactCTA] Secondary CTA clicked:', {
            text: secondaryBtn.textContent?.trim(),
            destination: secondaryBtn.getAttribute('href'),
          });
        }
      });
    }
  });
</script>
